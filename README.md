# tiny-rasterizer

You can rotate the cube with `w`, `a`, `s`, `d`, and `q`, `e`; reset its position with `space`; use the both of the `mouse wheels` to change focal distance and zoom; use `arrow keys` to shift your "position".